{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:errrecipient:displayName}}",
      "email": "{{seed:user:errrecipient:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:errrecipient:username}}"
    }
  }
}