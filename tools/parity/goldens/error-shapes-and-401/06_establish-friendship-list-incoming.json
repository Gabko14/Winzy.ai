{
  "status": 200,
  "body": {
    "incoming": [
      {
        "createdAt": "{{timestamp}}",
        "direction": "incoming",
        "fromDisplayName": "{{seed:user:errcreator:displayName}}",
        "fromUserId": "{{seed:user:errcreator:id}}",
        "fromUsername": "{{seed:user:errcreator:username}}",
        "id": "{{uuid:1}}"
      }
    ],
    "outgoing": []
  }
}