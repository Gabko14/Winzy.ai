{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:alice:displayName}}",
      "email": "{{seed:user:alice:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:alice:username}}"
    }
  }
}