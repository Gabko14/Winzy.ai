{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:searchable:displayName}}",
      "email": "{{seed:user:searchable:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:searchable:username}}"
    }
  }
}