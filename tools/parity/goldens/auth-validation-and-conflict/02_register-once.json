{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:conflict:displayName}}",
      "email": "{{seed:user:conflict:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:conflict:username}}"
    }
  }
}