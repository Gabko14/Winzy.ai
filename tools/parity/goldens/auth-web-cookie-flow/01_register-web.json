{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": null,
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:webuser:displayName}}",
      "email": "{{seed:user:webuser:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:webuser:username}}"
    }
  }
}