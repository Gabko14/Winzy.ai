{
  "status": 200,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": null,
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:webuser:displayName}}",
      "email": "{{seed:user:webuser:email}}",
      "id": "{{seed:user:webuser:id}}",
      "username": "{{seed:user:webuser:username}}"
    }
  }
}