{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:feeduser:displayName}}",
      "email": "{{seed:user:feeduser:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:feeduser:username}}"
    }
  }
}