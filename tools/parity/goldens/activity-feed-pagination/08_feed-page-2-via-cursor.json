{
  "status": 200,
  "body": {
    "hasMore": true,
    "items": [
      {
        "actorDisplayName": "{{seed:user:feeduser:displayName}}",
        "actorId": "{{seed:user:feeduser:id}}",
        "actorUsername": "{{seed:user:feeduser:username}}",
        "createdAt": "{{timestamp}}",
        "data": {
          "habitId": "{{uuid:1}}",
          "name": "Run",
          "userId": "{{seed:user:feeduser:id}}"
        },
        "eventType": "habit.created",
        "id": "{{uuid:2}}"
      },
      {
        "actorDisplayName": "{{seed:user:feeduser:displayName}}",
        "actorId": "{{seed:user:feeduser:id}}",
        "actorUsername": "{{seed:user:feeduser:username}}",
        "createdAt": "{{timestamp}}",
        "data": {
          "habitId": "{{uuid:3}}",
          "name": "Write",
          "userId": "{{seed:user:feeduser:id}}"
        },
        "eventType": "habit.created",
        "id": "{{uuid:4}}"
      }
    ],
    "nextCursor": "{{timestamp}}"
  }
}