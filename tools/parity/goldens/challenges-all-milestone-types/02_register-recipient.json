{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:challengerecipient:displayName}}",
      "email": "{{seed:user:challengerecipient:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:challengerecipient:username}}"
    }
  }
}