{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:statsuser:displayName}}",
      "email": "{{seed:user:statsuser:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:statsuser:username}}"
    }
  }
}