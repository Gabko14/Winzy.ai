{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:habitowner:displayName}}",
      "email": "{{seed:user:habitowner:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:habitowner:username}}"
    }
  }
}