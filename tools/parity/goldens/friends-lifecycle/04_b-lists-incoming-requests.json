{
  "status": 200,
  "body": {
    "incoming": [
      {
        "createdAt": "{{timestamp}}",
        "direction": "incoming",
        "fromDisplayName": "{{seed:user:friendA:displayName}}",
        "fromUserId": "{{seed:user:friendA:id}}",
        "fromUsername": "{{seed:user:friendA:username}}",
        "id": "{{uuid:1}}"
      }
    ],
    "outgoing": []
  }
}