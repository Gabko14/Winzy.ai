{
  "status": 200,
  "body": {
    "incoming": [
      {
        "createdAt": "{{timestamp}}",
        "direction": "incoming",
        "fromDisplayName": "{{seed:user:friendC:displayName}}",
        "fromUserId": "{{seed:user:friendC:id}}",
        "fromUsername": "{{seed:user:friendC:username}}",
        "id": "{{uuid:1}}"
      }
    ],
    "outgoing": []
  }
}