{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:friendA:displayName}}",
      "email": "{{seed:user:friendA:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:friendA:username}}"
    }
  }
}