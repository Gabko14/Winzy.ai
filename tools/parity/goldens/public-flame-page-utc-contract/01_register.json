{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:flameowner:displayName}}",
      "email": "{{seed:user:flameowner:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:flameowner:username}}"
    }
  }
}