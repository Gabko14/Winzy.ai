{
  "status": 200,
  "body": {
    "createdAt": "{{timestamp}}",
    "friendId": "{{seed:user:notifyobserver:id}}",
    "id": "{{uuid:1}}",
    "status": "accepted",
    "userId": "{{seed:user:notifyactor:id}}"
  }
}