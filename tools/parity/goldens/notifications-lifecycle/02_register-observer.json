{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:notifyobserver:displayName}}",
      "email": "{{seed:user:notifyobserver:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:notifyobserver:username}}"
    }
  }
}