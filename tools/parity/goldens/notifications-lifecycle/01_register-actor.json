{
  "status": 201,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:notifyactor:displayName}}",
      "email": "{{seed:user:notifyactor:email}}",
      "id": "{{uuid:1}}",
      "username": "{{seed:user:notifyactor:username}}"
    }
  }
}