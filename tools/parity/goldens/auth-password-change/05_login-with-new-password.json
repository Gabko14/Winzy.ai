{
  "status": 200,
  "body": {
    "accessToken": "{{token}}",
    "refreshToken": "{{token}}",
    "user": {
      "avatarUrl": null,
      "createdAt": "{{timestamp}}",
      "displayName": "{{seed:user:pwchange:displayName}}",
      "email": "{{seed:user:pwchange:email}}",
      "id": "{{seed:user:pwchange:id}}",
      "username": "{{seed:user:pwchange:username}}"
    }
  }
}