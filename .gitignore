.DS_Store

# bv (beads viewer) local config and caches
.bv/

# br (beads_rust) - SQLite database (JSONL is tracked, DB is not)
.beads/*.db
.beads/*.db-shm
.beads/*.db-wal
.beads/*.lock

# Migration artifacts
.beads-go-backup/
.beads/backup/backup/
