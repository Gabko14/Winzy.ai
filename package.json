{
  "name": "winzy-ai",
  "version": "0.0.0",
  "private": true,
  "description": "Winzy.ai mono-repo — git hooks and tooling",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx,js,jsx}": [
      "frontend/node_modules/.bin/eslint --no-warn-ignored --config frontend/eslint.config.mjs"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3"
  }
}
